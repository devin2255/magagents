__pycache__/
*.pyc
*.tmp
*.lock

# Secrets / local env
.env
.env.local

# Runtime data (regenerated by the app; keep dir via .gitkeep)
data/*.json
data/certificates/
!data/.gitkeep

# Local Claude Code config (launch.json etc.)
.claude/
